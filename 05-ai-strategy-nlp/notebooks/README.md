# Notebooks
- 01_data_extraction.ipynb: PDF to text; save cleaned text to data/processed.
- 02_nlp_analysis.ipynb: uses src.text_processing for cleaning and scikit-learn for TF-IDF.
- 03_visualization.ipynb: uses src.visualization to generate figures saved under results/figures.
Run notebooks in a virtualenv (python -m venv .venv && source .venv/bin/activate) and install requirements when added.
